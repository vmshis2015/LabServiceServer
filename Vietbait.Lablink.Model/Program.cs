using System.Data;

namespace Vietbait.Lablink.Model
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            //string _name = this.GetType().ToString();
            DataTable temptbl = null;
            int testTypeId = 0;
            string machineName = "CLINITEK";
            //DeviceHelper.GetDeviceConfig(machineName, ref testTypeId, ref temptbl);
        }
    }
}